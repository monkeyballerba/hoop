-- Wait for Octohook to load
repeat task.wait() until getgenv().library
local library = getgenv().library

-- Create main tab
local mainTab = library:AddTab("🏀 Arcade Basketball")

-- Vars
local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")

local player = Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local hum = char:WaitForChild("Humanoid")
local root = char:WaitForChild("HumanoidRootPart")

local autoGreen = false
local glideSpeed = 50

-- Auto Green Toggle
mainTab:AddToggle("Auto Green", {
    flag = "autoGreenToggle",
    default = false,
    callback = function(state)
        autoGreen = state
    end
})

-- Glide Speed Slider
mainTab:AddSlider("Glide Speed", {
    min = 10,
    max = 150,
    default = 50,
    textpos = 1,
    flag = "glideSpeedSlider",
    callback = function(val)
        glideSpeed = val
    end
})

-- Instructions Label
mainTab:AddLabel("Press [G] to Glide Forward")

-- Glide key
UIS.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.KeyCode == Enum.KeyCode.G then
        if root then
            root.Velocity = root.CFrame.LookVector * glideSpeed + Vector3.new(0, 20, 0)
        end
    end
end)

-- Hook for Auto Green (FireServer)
local old
old = hookmetamethod(game, "__namecall", newcclosure(function(self, ...)
    local method = getnamecallmethod()
    local args = { ... }

    if not checkcaller() and method == "FireServer" and tostring(self) == "Shoot" then
        if autoGreen and args[1] == false then
            args[2] = -0.98
        end
    end

    return old(self, unpack(args))
end))
